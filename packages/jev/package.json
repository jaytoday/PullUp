{
  "name": "@pullup/jev",
  "version": "0.1.0",
  "description": "PullUp's Jev adapters: the live TypeSafe AI SignalModel (typed per-hunk risk questions) plus a factory over live / synthetic / replay models.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@pullup/core": "workspace:*",
    "@typesafe-ai/sdk": "^0.6.0"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8"
  }
}
